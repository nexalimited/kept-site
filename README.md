# kept-site

Kept 隻 app 嘅私隱政策同支援頁 —— App Store 兩條必填 URL 指呢度。

⚠️ 呢個 repo 淨係得呢兩版靜態 HTML，**冇任何 app 源碼**（源碼喺 private 嘅 `nexalimited/Kept`）。
⚠️ 正本喺 `nexalimited/Kept` 個 `store/site/`，喺嗰邊改完再抄過嚟，唔好淨係改呢邊。
⚠️ 第日搬去 `kept.nexalimited.io` 根目錄之後，`support.html` 條 `privacy.html` 連結
   可以改返做 `/privacy`（而家係相對，因為 Pages 唔喺網域根目錄）。
